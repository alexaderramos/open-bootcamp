let alturaCentimetros = 180;
let alturaMetros = 1.80;
let pesoKilogramos = 80.5;
let alturaRedondeda = Math.round(alturaCentimetros);
let pesoRedondeado = Math.ceil(pesoKilogramos);
let val = "el máximo valor que se puede obtener en Javascript + 1 es: " + Number.MAX_VALUE + 1;